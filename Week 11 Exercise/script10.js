function calculate()
         {
            length = parseInt(prompt("Enter the length of rectangle "));
            breadth = parseInt(prompt("Enter the breadth of rectangle "));
            let r = length * breadth;
            alert("Area of rectangle = "+r);  
         }